# Frontend

## Requirements

- Node.js (optional, only if using a build tool)
- Any static server (http-server, live-server)

## Install dependencies

```bash
npm install
```

## Configuration

* Make sure the backend API URL is correct.

```
const API_BASE = 'http://localhost:3000';
```